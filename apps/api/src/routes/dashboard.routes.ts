import { Router } from 'express'; import { dashboard } from '../controllers/dashboard.controller.js'; import { authenticate } from '../middleware/authenticate.js'; import { requireRoles } from '../middleware/authorize.js'; export const dashboardRouter=Router(); dashboardRouter.get('/dashboard',authenticate,requireRoles('super_admin','admin','sales_manager'),dashboard);
